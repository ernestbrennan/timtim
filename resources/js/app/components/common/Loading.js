import React from 'react';

import RealtyListItem from '$app/components/realty-list/RealtyListItem';

function Loading() {
  return (
    <>
      <RealtyListItem data={{}} style={{}} index={1} />
      <RealtyListItem data={{}} style={{}} index={2} />
      <RealtyListItem data={{}} style={{}} index={3} />
      <RealtyListItem data={{}} style={{}} index={4} />
    </>
  );
}

export default Loading;
